{
  "name": "semantic-release-image",
  "dependencies": {
    "semantic-release": "^20.0.0"
  },
  "devDependencies": {
    "@google/semantic-release-replace-plugin": "github:google/semantic-release-replace-plugin",
    "@semantic-release/changelog": "github:semantic-release/changelog",
    "@semantic-release/git": "github:semantic-release/git",
    "commitizen": "^4.3.0",
    "conventional-changelog-conventionalcommits": "^5.0.0",
    "cz-conventional-changelog": "^3.3.0"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
